import { InputAutoresizeDirective } from "./input-autoresize.directive";

describe("InputAutoresizeDirective", () => {
  it("should create an instance", () => {
    const directive = new InputAutoresizeDirective();
    expect(directive).toBeTruthy();
  });
});
